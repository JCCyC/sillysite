#!/bin/sh
# Thin wrapper so the Java client can be invoked the same way as the C
# client's compiled ./login binary, rather than needing `java -cp ... Login`
# spelled out at every call site.
exec java -cp "$(dirname "$0")" Login "$@"
